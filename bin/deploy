#!/usr/bin/env bash
set -euo pipefail

dist="${1:-}"

if [[ -z "$dist" ]]; then
	echo "usage: $(basename "$0") DIST_DIR [rsdeploy args...]" >&2
	exit 1
fi

shift || true
exec rsdeploy -s "$dist/" -f .deploy-filter "$@"
