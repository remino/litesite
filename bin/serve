#!/usr/bin/env bash
set -euo pipefail

root="${1:-}"

if [[ -z "$root" ]]; then
  echo "usage: $(basename "$0") ROOT_DIR" >&2
  exit 1
fi

if [[ "$root" != /* ]]; then
  root="$(cd "$root" && pwd)"
fi

exec npx --yes live-server "$root/src/public" --host=0.0.0.0 --port="${PORT:-8080}" --no-browser
